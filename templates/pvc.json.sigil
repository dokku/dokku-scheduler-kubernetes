{
  "apiVersion": "v1",
  "kind": "PersistentVolumeClaim",
  "metadata": {
    "name": "{{ $.NAME }}"
  },
  "spec": {
    "accessModes": [
      "{{ $.ACCESS_MODE }}"
    ],
    "resources": {
      "requests": {
        "storage": "{{ $.STORAGE }}"
      }
    },
    "storageClassName": "{{ $.STORAGE_CLASS_NAME }}"
  }
}

{
  "apiVersion": "apps/v1",
  "kind": "PersistentVolumeClaim",
  "metadata": {
    "name": "{{ $.NAME }}",
    "labels": {
      "pvc": "{{ $.NAME }}"
    }
  },
  "spec": {
    "accessModes": [
      "{{ $.ACCESS_MODE }}"
    ],
    "resources": {
      "requests": {
        "storage": "{{ $.STORAGE }}"
      }
    },
    "storageClassName": "{{ $.STORAGE_CLASS_NAME }}"
  }
}

{
  "type": "Pods",
  "pods": {
    "metric": {
      "name": "{{ $.TARGET_NAME }}"
    },
    "target": {
      "type": "{{ $.TARGET_TYPE }}",
      "{{ $.TARGET_TYPE_KEY }}": {{ $.TARGET_VALUE }}
    }
  }
}
